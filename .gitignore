# Rust / Cargo
/target/
**/*.rs.bk

# skm project config (created by `skm init` in this repo)
.skm.toml

# Release packaging (release workflow)
/dist/

# Environment / secrets
.env
.env.*

# OS
.DS_Store
._*
Thumbs.db

# Editors / IDE (local MCP, etc.)
.idea/
.vscode/

# Local manual test / scratch dirs (may contain machine-specific paths)
.skm-manual-test*/
.skm-test-workdir
*.swp
*.swo
*~

# Coverage / profiling
coverage/
*.profraw

# Local Cargo overrides (cross-linker, etc.)
.cargo/config.toml
